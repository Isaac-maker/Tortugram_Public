package com.example.tortugram.ui.theme

import androidx.compose.ui.graphics.Color

// ---------------------------------------------------------------------------
// Paleta "Tortugram" — rediseño 2026
// Base neutra oscura (estilo Apple/Google) con un acento azul eléctrico
// fresco y moderno, reservado para foco, selección y estados interactivos.
// ---------------------------------------------------------------------------

// Neutros base (fondo y superficies): grises oscuros cálidos, nunca negro
// puro, para que bordes y sombras sigan siendo visibles.
val TortugramBackground = Color(0xFF121214)
val TortugramSurface = Color(0xFF1C1C1F)
val TortugramSurfaceVariant = Color(0xFF2A2A2E)
val TortugramOutline = Color(0xFF3A3A3E)

// Acento: azul eléctrico fresco, con un ligero toque violeta (indigo),
// para un look tecnológico y actual sin caer en el azul genérico.
val TortugramPrimary = Color(0xFF3F63F2)
val TortugramPrimaryLight = Color(0xFFA6B8FF)
val TortugramPrimaryDim = Color(0xFF25368C)

// Texto e iconografía: blancos y grises con alto contraste sobre los
// neutros oscuros de arriba.
val TortugramOnBackground = Color(0xFFF5F5F7)
val TortugramOnSurfaceVariant = Color(0xFFAEAEB4)
val TortugramOnPrimary = Color(0xFFFFFFFF)
